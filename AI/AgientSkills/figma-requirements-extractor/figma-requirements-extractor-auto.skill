---
name: figma-requirements-extractor-auto
description: 当用户提供 Figma 链接时，AI 自动提取设计稿数据并生成需求文档。无需手动运行脚本，AI 会自动执行所有步骤：提取 File Key、请求 Token（首次）、运行 Python 脚本、生成 Markdown 文档。适用于 Unity、Android、iOS 等移动端项目的需求对接。
---

# Figma 需求提取 Skill（AI 自动执行版）

## 概述

当用户提供 Figma 链接时，AI 会**自动**：
1. ✅ 识别并提取 File Key
2. ✅ 请求 Access Token（仅首次）
3. ✅ 运行 Python 提取脚本
4. ✅ 生成结构化需求文档
5. ✅ 呈现给用户并提供下载

**用户无需手动运行任何脚本！**

## 触发条件

AI 会在以下情况下**自动启动**此 Skill：

### 自动触发场景
- ✅ 用户消息中包含 Figma 链接（如 `https://www.figma.com/file/...`）
- ✅ 用户说"分析这个 Figma 设计稿"并提供链接
- ✅ 用户说"从这个 Figma 提取需求"
- ✅ 用户说"帮我生成需求文档" + Figma 链接

### 支持的链接格式
```
✅ https://www.figma.com/file/{FILE_KEY}/{FILE_NAME}
✅ https://www.figma.com/design/{FILE_KEY}/{FILE_NAME}
✅ https://www.figma.com/proto/{FILE_KEY}/{FILE_NAME}
✅ https://www.figma.com/board/{FILE_KEY}/{FILE_NAME}
```

## AI 自动执行流程

### 步骤 1: 识别 Figma 链接

**AI 检测到 Figma 链接后，立即：**

```
🔍 检测到 Figma 链接！

📄 文件: {从 URL 中提取的文件名}
🔑 File Key: {提取的 FILE_KEY}

正在准备提取设计稿数据...
```

### 步骤 2: 请求 Access Token（仅首次）

**如果是首次使用，AI 会显示：**

```
🔐 需要 Figma Access Token

为了访问 Figma API，我需要您的个人访问令牌。

📋 快速获取（30秒）：
1. 访问 https://www.figma.com/settings
2. 找到 "Personal access tokens" 部分
3. 点击 "Generate new token"
4. 命名为 "Claude Requirements"
5. 复制生成的 token（格式：figd_xxxxxxxxxxxx）

🔒 安全承诺：
• 仅在当前会话使用
• 不会被存储或记录  
• 仅用于读取设计稿数据

请输入您的 Token：
```

**如果已提供过 Token（在会话中），AI 会直接使用。**

### 步骤 3: 自动运行提取脚本

**AI 自动执行：**

1. **安装依赖**（如果需要）
```bash
pip install requests --break-system-packages
```

2. **创建或使用提取脚本**
   - 检查是否存在 `/home/claude/figma_extractor.py`
   - 如果不存在，自动创建脚本文件

3. **执行提取**
```bash
python /home/claude/figma_extractor.py <FILE_KEY> <TOKEN> --output-dir /home/claude/figma_output
```

4. **实时显示进度**
```
⏳ 正在提取 Figma 数据...

✅ 已获取文件基本信息
   文件名: {文件名}
   最后修改: {时间}

✅ 正在解析页面结构 (1/3)
✅ 正在解析页面结构 (2/3)  
✅ 正在解析页面结构 (3/3)

✅ 正在分析组件特征...
   识别到 45 个组件
   - 按钮: 12
   - 输入框: 5
   - 列表: 3
   - 状态显示: 8

✅ 正在智能推断接口依赖...
   生成了 15 个接口规格

🎉 提取完成！
```

### 步骤 4: 智能分析与确认

**AI 会展示识别结果并请求确认：**

```
📊 提取结果摘要：

**组件统计**
- 总组件数: 45
- 按钮: 12（其中控制类 5 个，提交类 3 个，切换类 4 个）
- 输入框: 5
- 列表: 3
- 状态显示: 8
- 图表: 2

**接口依赖识别**
- SDK 控制接口: 5 个（P0）
- 后端查询接口: 6 个（P1）
- 数据提交接口: 3 个（P1）
- 统计接口: 1 个（P2）

🤔 需要确认的推断：

1. "开始清扫"按钮 → 推断为【SDK控制接口 - P0】
   是否正确？

2. "速度显示"文本 → 推断需要【实时查询接口】
   更新频率是多少？
   [ ] 实时（1秒轮询）
   [ ] 定时（5秒轮询）
   [ ] 手动刷新

3. "历史记录"列表 → 推断为【后端分页接口】
   是否支持离线缓存？
```

**用户可以：**
- ✅ 确认所有推断
- ✅ 指出错误并让 AI 修正
- ✅ 补充遗漏的信息

### 步骤 5: 生成并呈现文档

**AI 自动生成 Markdown 文档并呈现：**

```
📝 需求文档已生成！

正在为您呈现文档...
```

**然后 AI 会：**
1. 将文档复制到 `/mnt/user-data/outputs/`
2. 使用 `present_files` 工具展示给用户
3. 提供文档内容的简要说明

## 生成的文档结构

AI 自动生成的 Markdown 文档包含：

### 1. 文档概览
```markdown
# {文件名} - 需求文档

> 📅 生成时间: 2024-02-12 10:30:00
> 🔗 Figma 文件: {FILE_KEY}
> 📝 最后修改: {最后修改时间}

## 📊 文档概览
- 总组件数: 45
- 识别的接口依赖: 15
- 纯前端任务: 18
- SDK/后端依赖: 12
```

### 2. 功能清单（三级分类）
```markdown
## 1️⃣ 功能清单

### 1.1 独立开发任务（纯前端实现）
- [ ] **导航栏** - FRAME - 页面切换逻辑
- [ ] **设置弹窗** - FRAME - 模态框交互
- [ ] **速度滑块** - 组件 - 本地UI反馈

### 1.2 SDK/接口依赖任务  
- [ ] **开始清扫按钮** (BUTTON) - "开始清扫"
- [ ] **电量显示** (STATUS_DISPLAY) - "电量: 80%"
- [ ] **历史记录列表** (LIST) - 分页数据展示

### 1.3 底层服务联调任务
- [ ] 验证机器人实际清扫动作
- [ ] 验证速度调节生效
- [ ] 验证紧急停止功能
```

### 3. 接口依赖详细规格
```markdown
## 2️⃣ 接口依赖详细规格

### 1. 控制_开始清扫 🔴

**依赖类型**: SDK封装 + 底层服务验证
**调用时机**: 用户点击'开始清扫'按钮时
**关联组件**: `开始清扫按钮`

**请求参数**:
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `action` | string | 开始清扫动作指令 |
| `mode` | string | 清扫模式（可选：standard/deep/edge） |

**返回数据**:
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `status` | string | 执行状态 success/failed |
| `task_id` | string | 任务ID（用于查询进度） |
| `message` | string | 结果描述 |

**验收标准**:
- [ ] SDK接口调用返回success
- [ ] 机器人实际开始清扫动作
- [ ] UI状态更新为"清扫中"
- [ ] 可以通过 task_id 查询进度

**优先级**: P0（核心功能，阻塞）

**联调对接人**: SDK团队 - 张三

---

### 2. 查询电池电量 🟡

**依赖类型**: SDK封装
**调用时机**: 页面加载时 / 定时轮询（建议5秒）
**关联组件**: `电量显示`

**请求参数**: 无

**返回数据**:
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `level` | int | 电量百分比 0-100 |
| `charging` | boolean | 是否正在充电 |
| `voltage` | float | 电压值（可选） |

**验收标准**:
- [ ] 电量显示与实际一致（误差<5%）
- [ ] 充电状态正确显示
- [ ] 数据更新及时

**优先级**: P1（重要但非阻塞）

---
```

### 4. 技术实现要点
```markdown
## 3️⃣ 技术实现要点

### 状态管理建议
- **全局状态**: 用户信息、设备连接状态、电量等
- **页面状态**: 当前选中 tab、弹窗显示等
- **缓存策略**: 电量、速度等频繁查询数据考虑本地缓存

### 数据流设计
用户操作 → UI事件 → Action → SDK/API → 响应 → State更新 → UI刷新

### 错误处理
- 网络异常: 显示错误提示 + 重试按钮
- 超时处理: 建议超时时间 5-10秒
- 降级方案: 关键功能失败时的备用方案
```

### 5. 联调计划
```markdown
## 4️⃣ 联调计划

### 三方对齐会议 Checklist
- [ ] 所有接口的请求/响应格式已确认
- [ ] 接口调用频率和性能要求已明确
- [ ] 错误码和异常处理机制已定义
- [ ] 测试环境已就绪
- [ ] Mock数据已准备

### 分阶段验证
1. **阶段1**: Mock数据 → UI开发
2. **阶段2**: 集成SDK → 接口联调
3. **阶段3**: 端到端测试 → 设备验证
4. **阶段4**: 性能优化 → 异常场景
```

### 6. 风险评估
```markdown
## 5️⃣ 风险点和依赖关系

### 阻塞性依赖 🔴
- **控制_开始清扫**: 核心功能，缺失将导致无法演示
- **控制_停止清扫**: 安全功能，必须优先实现

### 建议联调顺序
1. 优先对接 P0 接口（控制类）
2. 其次对接 P1 接口（状态查询）
3. 最后对接 P2 接口（统计图表）
```

## AI 交互示例

### 示例 1: 完整流程

**用户输入：**
```
请帮我分析这个 Figma 设计稿：
https://www.figma.com/file/AbCd1234EfGh5678/RobotControl
```

**AI 自动响应：**
```
🔍 检测到 Figma 链接！

📄 文件: RobotControl
🔑 File Key: AbCd1234EfGh5678

🔐 需要 Figma Access Token
[显示获取指引]

请输入您的 Token：
```

**用户提供 Token：**
```
figd_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**AI 自动执行并显示进度：**
```
⏳ 正在提取 Figma 数据...
✅ 已获取文件基本信息
✅ 正在解析页面结构 (1/2)
✅ 正在解析页面结构 (2/2)
✅ 正在分析组件特征...
   识别到 32 个组件
✅ 正在智能推断接口依赖...
   生成了 10 个接口规格
🎉 提取完成！

📊 提取结果摘要：
[显示统计信息]

🤔 需要确认的推断：
1. "开始清扫"按钮 → 推断为【SDK控制接口 - P0】
   是否正确？
```

**用户确认：**
```
看起来都正确
```

**AI 生成文档并呈现：**
```
✅ 已确认所有推断

📝 正在生成需求文档...
✅ 文档已生成！

[呈现 Markdown 文档]

📄 **RobotControl_需求文档.md**

您可以下载此文档并与团队共享。

需要我做什么调整吗？
```

### 示例 2: 快速模式（已有 Token）

**用户输入：**
```
https://www.figma.com/design/XyZ789/NewFeature
我的 token: figd_xxxxxxxxxxxx
```

**AI 自动响应：**
```
🔍 检测到 Figma 链接和 Token！

正在自动提取...

⏳ 提取进度：
✅ 获取文件数据
✅ 解析组件 (38个)
✅ 推断接口 (12个)
🎉 完成！

📝 需求文档已生成
[呈现文档]
```

### 示例 3: 处理错误

**Token 无效时：**
```
❌ Figma API 返回错误: 403 Forbidden

可能的原因：
• Token 已过期
• Token 复制时包含了空格
• 没有访问此文件的权限

请重新检查您的 Token，或者：
1. 生成新的 Token
2. 确认您有权限访问该文件
3. 让设计师将文件设置为"可查看"

请重新输入 Token：
```

## 智能识别规则

### UI 组件识别

AI 使用以下规则自动识别组件类型：

| Figma 组件特征 | 识别为 | 推断接口类型 |
|----------------|--------|-------------|
| 名称含 "button"/"btn"/"按钮" | BUTTON | 根据文本推断（控制/提交/切换） |
| 名称含 "input"/"textfield"/"输入" | INPUT | 可能需要验证接口 |
| 名称含 "list"/"列表" + 多个子元素 | LIST | 数据查询接口（支持分页） |
| 文本显示 "电量"/"battery" | STATUS_DISPLAY | 电量查询接口 |
| 文本显示 "速度"/"speed" | STATUS_DISPLAY | 速度查询接口 |
| 文本显示 "温度"/"temperature" | STATUS_DISPLAY | 温度查询接口 |
| 名称含 "chart"/"graph"/"统计" | CHART | 数据统计接口 |

### 接口依赖推断

| 按钮文本关键词 | 推断接口类型 | 优先级 |
|---------------|-------------|--------|
| "开始"/"启动"/"start" | SDK控制接口 | P0 |
| "停止"/"暂停"/"stop" | SDK控制接口 | P0 |
| "保存"/"提交"/"submit" | 后端API | P1 |
| "上传"/"upload" | 后端API | P1 |
| "切换"/"选择"/"tab" | 纯前端 | - |

## 错误处理与降级策略

### 常见错误及 AI 处理方式

#### 1. Token 无效
```
AI 行为：
- 清晰说明可能原因
- 提供解决方案
- 引导用户重新输入
```

#### 2. File Key 错误
```
AI 行为：
- 解释正确的链接格式
- 尝试从用户输入中提取
- 如果无法提取，请求正确链接
```

#### 3. 网络错误
```
AI 行为：
- 说明网络问题
- 建议重试
- 如果持续失败，提供手动方案
```

#### 4. 组件识别不准确
```
AI 行为：
- 展示识别结果
- 请求用户确认
- 根据反馈调整
- 允许人工补充
```

## 输出文件位置

AI 会将所有生成的文件放在：

```
/mnt/user-data/outputs/
├── {文件名}_需求文档.md          # 主要需求文档
├── figma_raw_data.json          # 原始API数据（可选）
└── component_mapping.json       # 组件映射（可选）
```

**然后使用 `present_files` 工具呈现给用户。**

## 最佳实践

### For AI

1. **主动引导** - 检测到 Figma 链接时立即行动
2. **透明进度** - 实时显示提取进度
3. **智能确认** - 对不确定的推断询问用户
4. **友好反馈** - 用 emoji 和清晰的格式展示信息
5. **错误处理** - 遇到错误时给出明确的解决方案

### For 用户

1. **提供清晰链接** - 直接粘贴 Figma 分享链接
2. **首次配置 Token** - 仅需配置一次（在会话内）
3. **确认推断结果** - 快速确认 AI 的推断是否正确
4. **补充业务逻辑** - AI 生成初稿，用户添加业务细节

## 总结

本 Skill 的核心价值：

1. **零手动操作** - 用户只需提供链接和 Token（首次）
2. **全自动执行** - AI 自动运行脚本、生成文档
3. **智能推断** - 70%+ 的接口规格自动生成
4. **即时呈现** - 文档直接呈现，无需下载查看
5. **持续优化** - 根据用户反馈不断调整

**用户体验：提供链接 → 等待 1-2 分钟 → 获得完整需求文档！**
